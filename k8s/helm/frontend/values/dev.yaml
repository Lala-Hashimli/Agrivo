replicaCount: 1

image:
  repository: harbor.example.com/agrivo/frontend
  tag: dev
  pullPolicy: IfNotPresent

ingress:
  enabled: true
  className: nginx
  host: dev.agrivo.example.com

resources:
  requests:
    cpu: 50m
    memory: 128Mi
  limits:
    cpu: 200m
    memory: 256Mi
